apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.basho.riak', name: 'riak-client', version: '2.0.0'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}